server:
  port: 9090

spring:
  application:
    name: Product-SERVICE


eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
       defaultZone: http://localhost:8763/eureka/
  instance:
    hostname: localhost
